using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("안녕 난 또치라고해 만나서 반가워");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
